// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Dominio real del sitio (usado por canonical, sitemap, etc.)
  site: 'https://lalanutricion.com',
});
